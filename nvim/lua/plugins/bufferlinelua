return {
    'akinsho/bufferline.nvim',
    version = "*",
    dependencies = 'nvim-tree/nvim-web-devicons', -- Required for file icons
    event = "VeryLazy",
    config = function()
        require('bufferline').setup({
            options = {
                -- Example option: set the style of the separators between tabs
                separator_style = "slant",
                mode = "tabs",
                -- Example option: show close icons for all buffers
                show_buffer_close_icons = true,
                -- You can add many more options here for customization
            },
            highlights = {
                fill = { bg = "None" },
                background = { bg = "None" },
                separator = { bg = "NONE" },
                separator_selected = { bg = "NONE" },
            }
        })
    end
}
